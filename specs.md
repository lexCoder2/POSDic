1. Multiuser - users should be able to change user easy (with a qr read) and keep other users session intact unless the other user closed, should be an indicator on the header with all the initiated session on a single browser

2. estimate cost - if a user dont know the cost of a product, when it was purchased, they can check an estimated cost to add a certain ranges and percentages to final calculations (close register, month statistics, etc.)

3. Only one sell option on the app - right now it exist 2 sell tabs, sell and quick sell. should be a one place with both options. If a user dont know the prices or , should have an easy-access way to add a generic product.

4. settings should only exist on the user dropdown options, not in the sidebar.

5. should exist a complete report system with export options to excel.

6. Feature to have two modes, current mode for sell and checkout on the same client, and another mode to create a ticket, register products and send that sell to another person with a different machine with only checkout (to mantain 2 positions, one to dispatch products and other that is cashier, that only handles money)

7. compact, clear design. Create a more compact, clean design, with less or no margins, less boxes, and less background diferences. small corner radius. no gradients, and clear and consistent elevations.

8. Robust and complete ticket system - the one exist right now its flaky and works different for each machine and ticket printer hardware. should produce consistent tickets with a complete and customizable template feature.

9. lists of shoping items. section to create shoping lists. search for product and add it. recomended based on quantity, and history of shopping depending on the day of the week. should have the feature to mark purchased items and use that information to help the register on the system.

10. use free services or local IA models to clasify products on categories with the name, type and brand .

11. integration to scan, or update recipts from providers, with smart capabilities (Automatic provider recognizer, and product count, and identification)
